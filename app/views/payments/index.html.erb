<% provide(:title, 'All Payments') %>

<% if admin? %>
  <% provide :actions, link_to(raw('<i class="icon-plus icon-white"></i> New Payment'), new_payment_path, class: "btn btn-large btn-primary") %>
<% end %>

<%= render 'layouts/pagination' %>

<table class="table table-striped table-bordered table-hover">
  <tr>
    <th>Code</th>
    <th>Invoice</th>
    <th>Player</th>
    <th>Amount</th>
    <th>Method</th>
    <th>Notes</th>
    <% if admin? %>
      <th></th>
      <th></th>
    <% end %>
  </tr>

  <%=render @payments %>

</table>

<%= render 'layouts/pagination' %>

<% provide :leftNavButtonBody, 'Invoices' %>
<% provide :leftNavButtonUrl, invoices_path %>
<% provide(:title, @team.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= @team.name %>
      </h1>
      <span>Sex: </span>
      <span>System ID: <%= @team.id%></span>
    </section>
  </aside>
</div>


<% if admin? %>
  <%= link_to 'Edit', edit_team_path(@team) %> |
<% end %>

<%= link_to 'List', teams_path %>

<% provide(:title, 'All Transactions') %>

<% if admin? %>
  <% provide :actions, link_to(raw('<i class="icon-plus icon-white"></i> New Transaction'), new_transaction_path, class: "btn btn-large btn-primary") %>
<% end %>

<%= render 'layouts/pagination' %>

<table class="table table-striped table-bordered table-hover">
  <tr>
    <th>Code</th>
    <th>Type</th>
    <th>Category</th>
    <th>Invoice</th>
    <th>Player</th>
    <th>Amount</th>
    <th>Date Paid</th>
    <th>Notes</th>
    <% if admin? %>
      <th></th>
      <th></th>
    <% end %>
  </tr>

  <%=render @transactions %>

</table>

<%= render 'layouts/pagination' %>

<% provide :leftNavButtonBody, 'Invoices' %>
<% provide :leftNavButtonUrl, invoices_path %>
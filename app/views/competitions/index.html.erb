<% provide(:title, 'All Competitions') %>
<h1><%=yield(:title) %></h1>

<%= will_paginate %>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <%=render @competitions %>

</table>

<br />

<% if admin? %>
  <%= link_to 'New Competition', new_competition_path %>
<% end %>

<%= will_paginate %>
<% provide(:title, 'All Competitions') %>

<%= will_paginate %>

<table class="table table-striped table-bordered table-hover">
  <tr>
    <th>Name</th>
    <% if admin? %>
      <th></th>
      <th></th>
    <% end %>
  </tr>

  <%=render @competitions %>

</table>

<%= will_paginate %>

<% if admin? %>
  <%= link_to(raw('<i class="icon-plus icon-white"></i> New Competition'), new_competition_path, class: "btn btn-large btn-primary") %>
<% end %>

<% provide :leftNavButtonBody, 'Home' %>
<% provide :leftNavButtonUrl, root_path %>

<% provide :rightNavButtonBody, 'Seasons' %>
<% provide :rightNavButtonUrl, seasons_path %>
<% provide(:title, 'All Competitions') %>
<h1><%=yield(:title) %></h1>

<%= will_paginate %>

<table class="table table-striped table-bordered table-hover">
  <tr>
    <th>Name</th>
    <% if admin? %>
      <th></th>
      <th></th>
    <% end %>
  </tr>

  <%=render @competitions %>

</table>

<% if admin? %>
  <%= link_to('New Competition', new_competition_path, class: "btn btn-large btn-primary") %>
<% end %>

<% provide :rightNavButtonBody, 'Seasons' %>
<% provide :rightNavButtonUrl, seasons_path %>

<%= will_paginate %>
<% provide(:title, 'New Division') %>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@division, html: {class: "form-horizontal well"}) do |f| %>

      <%=render 'fields', f: f %>

      <div class="form-actions">
        <%= button_tag(type: :submit, class: "btn btn-large btn-success") do %>
          <i class='icon-ok'></i> Create Division
        <% end %>
        <%= link_to raw("<i class='icon-remove'></i> Cancel"), divisions_path, class: "btn btn-large" %>
      </div>
    <% end %>
  </div>
</div>

<% provide :leftNavButtonBody, 'Divisions' %>
<% provide :leftNavButtonUrl, divisions_path %>
<% provide(:title, 'New Season') %>
<h1><%=yield(:title) %></h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@season, html: {class: "form-horizontal well"}) do |f| %>

      <%=render 'fields', f: f %>

      <div class="form-actions">
        <%= f.submit "Create Season", class: "btn btn-large btn-primary btn-success" %>
        <%= link_to 'Cancel', seasons_path, class: "btn btn-large" %>
      </div>
    <% end %>
  </div>
</div>

<% provide :leftNavButtonBody, 'Seasons' %>
<% provide :leftNavButtonUrl, seasons_path %>
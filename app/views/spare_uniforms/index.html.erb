<% provide(:title, 'All Spare Uniforms') %>

<% if admin? %>
  <% provide :actions, link_to(raw('<i class="icon-plus icon-white"></i> New Spare Uniform'), new_spare_uniform_path, class: "btn btn-large btn-primary") %>
<% end %>

<%= render 'layouts/pagination' %>

<table class="table table-striped table-bordered table-hover">
  <tr>
    <th>Number</th>
    <th>Sex</th>
    <th>Size</th>
    <% if admin? %>
      <th></th>
      <th></th>
    <% end %>
  </tr>

  <%=render @spare_uniforms %>

</table>

<%= render 'layouts/pagination' %>

<% provide :leftNavButtonBody, 'Home' %>
<% provide :leftNavButtonUrl, root_path %>

<% provide :rightNavButtonBody, 'Uniform Numbers' %>
<% provide :rightNavButtonUrl, uniform_numbers_path %>